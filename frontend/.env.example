# Frontend .env.local — copy to frontend/.env.local
# Default targets the backend on localhost:3001 (i.e. `docker compose up -d postgres`
# and `cd backend && bun run dev`, or `docker compose up` with the backend service).

BACKEND_URL=http://localhost:3001
